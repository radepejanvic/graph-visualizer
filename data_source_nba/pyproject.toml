[project]
name = "dsnba"
version = "0.1"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project.entry-points."data_sources"]
dsnba = "data_source_nba.data_source_nba:NBADataSource"
